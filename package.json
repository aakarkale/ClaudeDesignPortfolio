{
  "name": "aakar-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Aakar Kale — Senior Product Manager portfolio",
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --minify --format=iife --define:process.env.NODE_ENV=\\\"production\\\" --outfile=dist/bundle.js",
    "dev": "esbuild src/main.jsx --bundle --format=iife --sourcemap --outfile=dist/bundle.js --watch --servedir=."
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
